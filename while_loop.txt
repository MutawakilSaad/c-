Create a C# console application that uses a while loop to repeatedly ask the user to enter the word "stop". The loop should continue until the user enters "stop". Once "stop" is entered, the application should print "Exiting..." and terminate.

Hint: You'll need a variable to store the user's input within the loop. The while loop's condition should check if the entered word is not equal to "stop".
